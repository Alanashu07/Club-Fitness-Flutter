import 'package:flutter/material.dart';

class MemberManagerView extends StatelessWidget {
  const MemberManagerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('MemberManager View')),
    );
  }
}
