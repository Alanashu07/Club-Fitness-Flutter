import 'package:flutter/material.dart';

class AdminUtilsView extends StatelessWidget {
  const AdminUtilsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('AdminUtils View')),
    );
  }
}
