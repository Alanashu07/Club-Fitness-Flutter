import 'package:flutter/material.dart';

class WorkoutManagerView extends StatelessWidget {
  const WorkoutManagerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('WorkoutManager View')),
    );
  }
}
